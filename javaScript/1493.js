/**
 * @param {number[]} nums
 * @return {number}
 */
var longestSubarray = function(nums) {
    let pervous = 0
    let next = 0
    let max_length = 0
    for(let i = 0; i <= nums.length; i++){
        pervous++
        if (nums[i] == 0) {
            
        }
    }

};